server:
  port: ${SERVER_PORT:0}

spring:
  application:
    name: ${SPRING_APPLICATION_NAME:${project.artifactId}}

  messages:
    basename: i18n/messages
    encoding: UTF-8
    fallback-to-system-locale: false

  jpa:
    open-in-view: false