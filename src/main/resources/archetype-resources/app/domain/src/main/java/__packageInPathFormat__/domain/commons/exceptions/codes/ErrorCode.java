package ${package}.domain.commons.exceptions.codes;

public enum ErrorCode {
    EMPTY_VALUE,
    INVALID_VALUE,
    MISSING_REQUIRED_FIELD,

    NOT_FOUND,
    CONFLICT,
    FORBIDDEN,

    INTERNAL_ERROR
}